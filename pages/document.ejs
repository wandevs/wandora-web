<!doctype html>
<html>

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no">
  <link href="https://fonts.googleapis.com/css?family=Roboto+Condensed:300,400,700&display=swap" rel="stylesheet">
  <link rel="icon" type="image/png" sizes="16x16" href="./static/wandoraLogo.421135be.png" />
  <title>Wandora Box</title>
  <script>
    function getExplorer() {
      var explorer = window.navigator.userAgent;
      var ie11 = ("ActiveXObject" in window);
      function compare(s) {
        return (explorer.indexOf(s) >= 0);
      }
      if (ie11 || compare("MSIE") || compare("Edge")) {
        return 'ie';
      } else if (compare("Firefox")) {
        return 'Firefox';
      } else if (compare("Chrome")) {
        return 'Chrome';
      } else if (compare("Opera")) {
        return 'Opera';
      } else if (compare("Safari")) {
        return 'Safari';
      } else {
        return 'Unknown';
      }
    }

    if (getExplorer() === 'ie') {
      alert('To ensure your asset security, Chrome, Firefox or Safari is recommended.');
    } 
  </script>
</head>

<body>
  <div id="root"></div>
</body>

</html>